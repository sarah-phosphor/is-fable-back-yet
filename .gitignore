# local env (the news feed is keyless now, but keep envs out of git anyway)
.env
.env.*

# netlify
.netlify/

# node
node_modules/

# handoff notes (kept local / off the public repo by default)
HANDOFF.md
